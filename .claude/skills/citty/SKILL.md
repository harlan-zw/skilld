---
name: citty
description: Documentation for citty. Use this skill when working with citty or importing from "citty".
version: "0.1.6"
---

Done. The skill extracts:

**API Reference:** All 7 functions with signatures and use cases
- `defineCommand` (type helper)
- `runMain` (recommended entry point)
- `createMain`, `runCommand`, `parseArgs`, `renderUsage`, `showUsage`

**Argument System:** Type-specific handling (positional, string, boolean) with the exact shape needed

**Integration:** How sub-commands work, async/lazy loading patterns, entry point structure

**Gotchas:** 
- Positional arg ordering requirements
- Boolean flag syntax (no `=` values)
- Alias behavior
- Exit code defaults
- `--` parsing stop
- Sub-command exclusivity

Removed badges, installation, changelog, generic intro, and external links. Output is in `/tmp/claude-1000/-home-harlan-pkg-skilld/9cfe25a0-c8eb-4819-bb9d-d6ffcb940de8/scratchpad/SKILL.md`